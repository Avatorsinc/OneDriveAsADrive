# Anchored to the repo root so the .NET build output is ignored but npm/bin/ (the CLI) is not.
/bin/
/obj/
/publish/
*.user
.vs/
.idea/
*.suo
launchSettings.json
Properties/launchSettings.json
appsettings.Development.json

# wingetcreate output - these files get PR'd to microsoft/winget-pkgs, not committed here
/manifests/

# Inno Setup compiler output
/installer/Output/

# code-signing material must never enter a repo
*.pfx
*.p12
